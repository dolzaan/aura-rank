import { posts } from '@/lib/mock';
export default function Feed(){return <section><h1>Feed de aura</h1><p className="muted">Os farms mais recentes da comunidade.</p><div style={{display:'grid',gap:16}}>{posts.map((p,i)=><article className="panel" style={{padding:20}} key={i}><div style={{display:'flex',justifyContent:'space-between'}}><b>@{p.user.username}</b><strong className="lime">+{p.points.toLocaleString('pt-BR')} AURA</strong></div><div style={{height:340,borderRadius:18,margin:'16px 0',background:'linear-gradient(135deg,#263529,#0d130f)',display:'grid',placeItems:'center'}}><span className="muted">Prévia do vídeo</span></div><p>{p.caption}</p><div style={{display:'flex',gap:8,flexWrap:'wrap'}}>{p.tags.map(t=><span key={t} className="btn-secondary" style={{padding:'7px 10px',borderRadius:999}}>{t}</span>)}</div></article>)}</div></section>}
