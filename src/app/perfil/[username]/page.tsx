import { posts, users } from '@/lib/mock';
export default async function Perfil({params}:{params:Promise<{username:string}>}){const {username}=await params;const user=users.find(u=>u.username===username)??users[2];return <section><div className="panel" style={{padding:28,display:'flex',gap:20,alignItems:'center',flexWrap:'wrap'}}><div style={{width:92,height:92,borderRadius:99,background:'#223127',display:'grid',placeItems:'center',fontSize:28,fontWeight:900}}>{user.avatar}</div><div style={{flex:1}}><h1 style={{marginBottom:4}}>{user.name}</h1><div className="muted">@{user.username}</div><p>Construindo reputação aurística, um vídeo por vez.</p></div><div><div className="lime" style={{fontSize:30,fontWeight:900}}>{user.aura.toLocaleString('pt-BR')}</div><div className="muted">aura total</div></div></div><h2>Melhores farms</h2>{posts.slice(0,1).map((p,i)=><article key={i} className="panel" style={{padding:20}}><strong className="lime">+{p.points} aura</strong><p>{p.caption}</p></article>)}</section>}
