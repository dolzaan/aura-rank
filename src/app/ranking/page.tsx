import { users } from '@/lib/mock';
export default function Ranking(){return <section><h1>Ranking global</h1><p className="muted">Temporada atual · julho de 2026</p><div className="panel" style={{overflow:'hidden'}}>{users.map((u,i)=><div key={u.username} style={{display:'grid',gridTemplateColumns:'50px 1fr auto',gap:12,alignItems:'center',padding:18,borderBottom:'1px solid #213027'}}><b className="lime">#{i+1}</b><div><strong>{u.name}</strong><div className="muted">@{u.username} · {u.trend} esta semana</div></div><strong>{u.aura.toLocaleString('pt-BR')}</strong></div>)}</div></section>}
