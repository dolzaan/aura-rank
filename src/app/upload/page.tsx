'use client';
import { useState } from 'react';
export default function Upload(){const [result,setResult]=useState<number|null>(null);const [loading,setLoading]=useState(false);async function analyze(){setLoading(true);await new Promise(r=>setTimeout(r,900));setResult(847);setLoading(false)}return <section style={{maxWidth:720,margin:'0 auto'}}><h1>Farmar aura</h1><p className="muted">Envie um vídeo de até 45 segundos. No modo de demonstração, a análise usa uma pontuação simulada.</p><div className="panel" style={{padding:24,display:'grid',gap:14}}><label><b>Vídeo</b><input className="input" type="file" accept="video/*" style={{marginTop:8}}/></label><label><b>Legenda</b><textarea className="input" rows={4} placeholder="Conte o contexto do farm..." style={{marginTop:8}}/></label><button className="btn" onClick={analyze} disabled={loading}>{loading?'Analisando presença...':'Enviar para análise'}</button>{result!==null&&<div className="panel" style={{padding:20,textAlign:'center'}}><div className="lime" style={{fontSize:42,fontWeight:900}}>+{result} AURA</div><p>Entrada confiante e bem executada.</p><small className="muted">Conecte Gemini e Vercel Blob para análise real em produção.</small></div>}</div></section>}
